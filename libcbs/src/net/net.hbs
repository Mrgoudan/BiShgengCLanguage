#ifndef BISHENG_C_SAFETY_NET_H
#define BISHENG_C_SAFETY_NET_H

#include <stdint.h>

struct Ipv4Addr {
    uint8_t octets[4];
};

_Safe struct Ipv4Addr struct Ipv4Addr::new(uint8_t a, uint8_t b, uint8_t c, uint8_t d);
_Safe uint32_t struct Ipv4Addr::to_bits(This this);

#endif
