// RUN: %clang_cc1 -internal-isystem %S/../../../../../../lib/Headers/bsc_include -verify %s 

#include "../../async-func-read-opposite.hbs"

_Trait Future<int>* f() { // expected-error {{_Await expression is not allowed to appear in non-_Async function.}}
    _Trait Future<int>*f;
    int a = _Await read(1);
    return f;
}

int main() {
  return 0;
}