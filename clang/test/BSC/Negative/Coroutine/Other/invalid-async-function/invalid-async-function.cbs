// RUN: %clang_cc1 -internal-isystem %S/../../../../../../lib/Headers/bsc_include -verify %s 

#include "../../async-func-read-opposite.hbs"

_Async _Trait Future<int>* f1() { // expected-error {{function which returns 'Future' type should not be modified by '_Async'}}
    _Trait Future<int>* f;
    int a = _Await read(1);
    return f;
}

int main() {
  return 0;
}