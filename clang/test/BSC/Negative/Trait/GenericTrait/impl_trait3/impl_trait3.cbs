// RUN: %clang_cc1 -fsyntax-only -verify %s

_Trait F<T> {
  T foo(This* this);
};

int int::foo(int* this) {
  return 10;
}

_Impl _Trait F<S> for int; // expected-error {{use of undeclared identifier 'S'}}