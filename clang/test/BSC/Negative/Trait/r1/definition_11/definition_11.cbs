// RUN: %clang_cc1 -fsyntax-only -verify %s

_Trait F {
  int foo(This* this);
};

int int::foo(int* this) {
  return 0;
}

_Impl _Trait F for int;

int main() {
  int a = 1;
  _Trait F* f = &a;
  f->get(); // expected-error {{no member named 'get' in 'struct __Trait_F_Vtable'}}
  return 0;
}
