// RUN: %clang_cc1 -fsyntax-only -verify %s

typedef _Trait F {
  int foo(This* this, int a);
}G;

typedef _Trait G { // expected-warning {{typedef requires a name}}
  int foo(This* this);
};

struct S{
  int x;
};

int struct S::foo(struct S* this, int a) {
  return this->x + a;
}

_Impl G for struct S;