// RUN: %clang_cc1 -verify %s

_Owned struct A {    // expected-note {{previous definition is here}}
  int ~A(A this) { // expected-error {{destructor cannot have a return type}}
  }

};


_Owned struct A {    // expected-error {{redefinition of 'A'}}
  void ~A(A this) { // expected-error {{destructor cannot have a return type}}
  }

};