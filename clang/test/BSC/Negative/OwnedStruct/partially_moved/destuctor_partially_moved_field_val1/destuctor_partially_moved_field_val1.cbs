// RUN: %clang_cc1 -verify %s

_Owned struct A {

  };
  
  _Owned struct S {
  _Public:
      A a;
      char a2;

  };
  
  int foo() {
    S s1 = {};
    A a = s1.a;
    return 1;// expected-error {{partially moved _Owned struct:`s1` at scope end, s1.a is moved}}
  }

  int main() {
    foo();
    return 0;
  }