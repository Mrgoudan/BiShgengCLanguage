// RUN: %clang_cc1 -verify %s

_Owned struct A {
    int a;
};

A A::init_a1(){
  A d ={2}; // expected-error {{need explicit constructor because '_Owned struct A' has private field}}
 return d;
}

int main() {
    A b = {.a=1}; // expected-error {{need explicit constructor because '_Owned struct A' has private field}}
    return 0;
}
