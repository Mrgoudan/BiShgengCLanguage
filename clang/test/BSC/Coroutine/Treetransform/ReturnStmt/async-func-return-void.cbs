// RUN: %clang %s -o %test.output
// RUN: %test.output
// expected-no-diagnostics

#include "../../async-func-read.hbs"

async void f(int start) {
  int result = start;
  for (int i = 0; i < start; i++) { 
     int a = await read(1);
  }
}

int main() {
  struct _Futuref this = {}; 
  struct _Futuref::poll(&this);
  return 0;
}