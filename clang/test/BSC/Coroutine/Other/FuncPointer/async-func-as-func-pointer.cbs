// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output


#include "../../async-func-read.hbs"

async int g(int start) {
    return 0;
}

struct __FatPointer_int (*CallBack)(int) = g;
async int f(int start) {   
  int result = await CallBack(1);
  return result;
}

int main() {
  return 0;
}