// RUN: %clang_cc1 -verify %s 

#include "../async-func-read-opposite.hbs"

struct __FatPointer<int> f() { // expected-error {{await expression is not allowed to appear in non-async function.}}    
    struct __FatPointer<int> f;
    int a = await read(1);
    return f;
}

int main() {
  return 0;
}