// RUN: %clang %s -o %test.output
// RUN: %clang -rewrite-bsc %S/../../../../../../lib/Headers/bsc_include/future.hbs -o %T/future.h
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// expected-no-diagnostics

#include "future.hbs"
#include <stdlib.h>

_Async volatile char g(char x) {
    char a = _Await g(1);
    switch (1) {
        case 1:
            return _Await g(2);
            break;
        case 2:
            return _Await g(3);
            break;
        default:
            return _Await g(4);
    }
    return 0;
}

int main()
{
    _Trait Future<volatile char>* this = g(1);
    this->poll();
    return 0;
}