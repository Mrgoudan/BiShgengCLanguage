// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %S/../../../../../../../lib/Headers/bsc_include/future.hbs -o %T/future.h
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

#include "future.hbs"
#include <stdlib.h>

_Async int int::g(int start) {
    return 0;
}

_Async int int::q(int* this) {
    return 0;
}

_Trait Future<int>* (*CallBack)(int) = int::g;
_Trait Future<int>* (*CallBack2)(int*) = int::q;

_Async int f(int start) {
  int result = _Await CallBack(1);
  result = _Await CallBack2(&result);
  return result;
}

int main() {
  return 0;
}