// RUN: %clang_cc1 -fsyntax-only -verify -x bsc %s
// expected-no-diagnostics

typedef struct {
  unsigned int bits[256];
} bit_stream;

_Safe void* memset(void *borrow dest,  int sizeMax, unsigned long c);

bit_stream *stram = nullptr;
_Safe void bar(int n) {
  _Unsafe { memset(stram[n].bits, 0, 0); }
}
int main() { return 0; }