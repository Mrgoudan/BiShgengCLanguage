// RUN: %clang_cc1 -nullability-check=all -verify %s
// expected-no-diagnostics

void test_move_to_raw_null_expr_symmetry(int *_Nullable p) {
  if (p != __move_to_raw(__take_from_raw((int *)0))) {
    *p = 1;
  }
}
