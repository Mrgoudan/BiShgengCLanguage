// RUN: %clang %s -o %t.output
// RUN: %t.output
// expected-no-diagnostics

struct M<int N, T> {
    T arr[N];
};

int struct M<int N, T>::f() {
    return 1;
}

constexpr int int::f() {
    return 0;
}

constexpr int f() {
    return 0;
}

struct A<int N> {

};

int main() {
    struct M<int::f(), struct A<int::f()> > b1;
    struct M<f(), int>::f();
    return 0;
}