// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

#define NULL (void *)0

void bug(const int *borrow p) {
  if (p == (const int *borrow)NULL) {
    return;
  }
}

int main(void) {
  return 0;
}