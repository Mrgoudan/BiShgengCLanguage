// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

void test(int *_Borrow p) {
  int *_Borrow arr[2] = {p};
}

int main(){
    int * _Borrow a = (int* _Borrow)malloc(sizeof(int));
    test(a);
    return 0;
}
