// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

void test(int *borrow p) {
  int *borrow arr[2] = {p};
}

int main(){
    int * borrow a = (int* borrow)malloc(sizeof(int));
    test(a);
    return 0;
}
