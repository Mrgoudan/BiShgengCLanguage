// RUN: %clang_cc1 -ast-dump -verify %s

int owned a;    //expected-error {{type of global variable cannot be 'owned'}}
owned int b;    //expected-error {{type of global variable cannot be 'owned'}}

int owned* p1;  //expected-error {{type of global variable cannot be 'owned'（even indirectly), 'owned int *' contains 'owned' type}}
int* owned p3;  //expected-error {{type of global variable cannot be 'owned'}}

int** owned p4; //expected-error {{type of global variable cannot be 'owned'}}

int* owned* p5; //expected-error {{type of global variable cannot be 'owned'（even indirectly), 'int *owned *' contains 'owned' type}}
owned int** p6; //expected-error {{type of global variable cannot be 'owned'（even indirectly), 'owned int **' contains 'owned' type}}