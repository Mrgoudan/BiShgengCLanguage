// RUN: %clang_cc1 -fsyntax-only -verify %s

trait F {
  int foo(This* this);
};

impl trait F for trait F; /// definition_10.cbs:7:12: error: can not find 'foo' of 'trait F' in 'trait F'

int main() {
  return 0;
}
